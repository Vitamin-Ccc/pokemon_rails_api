import React from "react";

const PokemonForm = () => {

  const handleSubmit = (e) => {

  };
  
  return (
    <form onSubmit = {handleSubmit}>
      <p>Name</p>
      <input />
      <button>Add</button>
    </form>
  );
};

export default PokemonForm;